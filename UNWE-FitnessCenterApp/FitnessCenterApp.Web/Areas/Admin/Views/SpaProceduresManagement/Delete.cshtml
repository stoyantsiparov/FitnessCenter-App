@model FitnessCenterApp.Web.ViewModels.SpaProcedure.DeleteSpaProcedureViewModel

@{
	ViewData["Title"] = "Delete Spa Procedure";
}

<div class="container mt-5">
	<h2 class="text-center mb-4 text-danger">@ViewData["Title"]</h2>

	<div class="row justify-content-center">
		<div class="col-md-6">
			<div class="card shadow border-0">
				<div class="card-body p-4 text-center">
					<i class="fas fa-exclamation-triangle fa-3x text-danger mb-3"></i>
					<h4>Are you sure you want to delete this spa procedure?</h4>
					<p class="text-muted mb-4">@Model.Name</p>

					<form asp-action="Delete" method="post">
						<input type="hidden" asp-for="Id" />
                        
						<button type="submit" class="btn btn-danger w-100 mb-2">
							<i class="fas fa-trash"></i> Yes, Delete
						</button>
						<a asp-action="Index" class="btn btn-secondary w-100">
							<i class="fas fa-arrow-left"></i> Cancel
						</a>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>