@model IEnumerable<FitnessCenterApp.Web.ViewModels.FitnessClass.AllFitnessClassesViewModel>

@{
    ViewData["Title"] = "Управление на фитнес класове";
}

<div class="container mt-5">
    <h2 class="text-center mb-4 text-primary">@ViewData["Title"]</h2>

    <div class="d-flex justify-content-between mb-3">
        <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Обратно към таблото
        </a>
        <div class="d-flex gap-2">
            <a asp-action="ExportClassesToCsv" class="btn btn-success">
                <i class="fas fa-file-csv"></i> Експорт (CSV)
            </a>
            <a asp-action="Add" class="btn btn-primary">
                <i class="fas fa-plus"></i> Добави фитнес клас
            </a>
        </div>
    </div>

    <div class="table-responsive shadow-sm pb-3">
        <table id="dataTable" class="table table-bordered table-striped table-hover mb-0">
            <thead class="table-dark text-center">
                <tr>
                    <th style="width: 100px;">Снимка</th>
                    <th>Име на клас</th>
                    <th>График</th>
                    <th>Продълж.</th>
                    <th>Капацитет</th>
                    <th style="min-width: 320px;">Действия</th>
                </tr>
            </thead>
            <tbody class="align-middle">
                @if (Model == null || !Model.Any())
                {
                    <tr>
                        <td colspan="6" class="text-center text-muted py-4">Не са намерени фитнес класове.</td>
                    </tr>
                }
                else
                {
                    @foreach (var classModel in Model)
                    {
                        <tr>
                            <td class="text-center">
                                @if (!string.IsNullOrEmpty(classModel.ImageUrl))
                                {
                                    <img src="@classModel.ImageUrl" alt="@classModel.Name" class="img-thumbnail" style="width: 60px; height: 60px; object-fit: cover;" />
                                }
                                else
                                {
                                    <img src="~/img/no-image-available-02.jpg" alt="Няма снимка" class="img-thumbnail" style="width: 60px; height: 60px; object-fit: cover;" />
                                }
                            </td>
                            <td class="fw-bold">@classModel.Name</td>
                            <td class="text-center">@classModel.Schedule</td>
                            <td class="text-center">@classModel.Duration мин</td>
                            <td class="text-center">@classModel.Capacity места</td>
                            <td>
                                <div class="d-flex justify-content-center align-items-center gap-2 flex-nowrap">
                                    <a asp-action="Participants" asp-route-id="@classModel.Id" class="btn btn-info btn-sm text-nowrap">
                                        <i class="fas fa-users"></i> Участници
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@classModel.Id" class="btn btn-warning btn-sm text-nowrap">
                                        <i class="fas fa-edit"></i> Редактирай
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@classModel.Id" class="btn btn-danger btn-sm text-nowrap">
                                        <i class="fas fa-trash"></i> Изтрий
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.7/css/dataTables.bootstrap5.min.css" />
    <script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.7/js/dataTables.bootstrap5.min.js"></script>
    <script>
        $(document).ready(function () {
        $('#dataTable').DataTable({
        "paging": true,
        "ordering": true,
        "info": true,
        "order": [],
        "columnDefs": [
        { "orderable": false, "targets": [0, -1] }
        ],
        "language": {
        "url": "//cdn.datatables.net/plug-ins/1.13.7/i18n/bg.json"
        }
        });
        });
    </script>
}